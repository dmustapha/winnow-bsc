# Stress Test Report — Winnow
> Generated by hackathon-stress | 2026-09-09T08:06Z
> Confidence Score: 92/100 — Battle-tested
> Resume run: pre-crash session wrote the plan + 2 fix commits (c0e1ffd, e03ce37) but recorded no test evidence; this run re-executed the full matrix against the fixed code.

## Summary
| Metric | Value |
|--------|-------|
| Total tests | 65 |
| Passed | 65 |
| Failed (then fixed) | 0 this run (2 fix commits from pre-crash round re-verified) |
| Unresolved | 0 |
| Skipped (declared) | 8 classes |
| Confidence | 92/100 |

## Coverage Declaration
| Tier | Features | Coverage Achieved | Skipped Edge Case Classes |
|------|----------|-------------------|---------------------------|
| P0 | 10 (landing, category, agent detail, 4 POST APIs, agents/stats APIs, probe engine, worker) | Exhaustive — malformed/boundary/injection/race/load/recovery classes all executed | — |
| P1 | 3 (proof page, sessions API, a2a card) | Sampled — happy path + traversal/long-name/param adversarial | network throttle |
| P2 | visual/a11y/network-emu | Smoke via design_forge screenshots (same day) | 5-viewport sweep, full a11y, slow-3G, Playwright journeys, localStorage corruption, live activate (burns grant), live revoke (session 5 untouchable), LLM cost-blowup live |

## Phase Results

### Regression (post-design_forge)
- tsc --noEmit clean (first failure was TS6053 stale `.next/types` caused by this run's own concurrent `next build` — re-ran clean after `.next` regen; not a code bug)
- `next build` exit 0
- All 4 pages 200: `/` `/c/health-factor` `/agent/56/49637` `/proof`
- `/api/stats` carries non-empty `gradedByCat` (4 categories)

### Phase 4: Backend API (adversarial)
43-case battery, **zero 500s**: string/negative/overflow/NaN/60KB/empty/non-JSON bodies -> 400 on all 4 POST routes; wrong method -> 405; SQLi + XSS + LIKE-injection on `?q=`/`?cat=` -> 200 safe (row counts sane, integrity ok after); `page=-1|1e308|NaN` -> 400, `page=999999` -> empty 200; a2a path traversal + 2000-char name -> 404. Ghost agent reprobe -> honest `404 {"error":"unknown agent"}`. Concurrency: 2 parallel reprobes same agent -> one live grade (C/59, 2.17s), one `429 cooldown 20s`. 20-concurrent `/api/agents` -> all 200, worst 0.14s.

### Phase 2.5: End-to-End Flows
| Flow | Duration | Final State | Result |
|------|----------|-------------|--------|
| reprobe live grade | 2.17s / ≤15s | grades row probe_log_id=2856 persisted (sqlite-confirmed) | PASS |
| worker background grading | 25 grades in ~18s | grades monotonic, verify-claims exit 0 | PASS |

### Phase 5: Contract (session ops, validation + safe live)
- Overcap-demo session 5: on-chain **ExceededSpendLimit revert** in 2.5s, session remains valid; immediate repeat -> 429 (60s cooldown); ghost session -> 404. Session 5 never revoked.
- Live activate/revoke deliberately OUT_OF_SLICE (grant burn / demo-critical session).

### Phase 5.5: AI-Agent Failure Modes
| Edge class | Result |
|-----------|--------|
| Tool hallucination / dead endpoint | PASS — garbage-endpoint agent graded **F/17** in 16ms, honest failure, rows cleaned |
| Timeout | PASS — `AbortSignal.timeout(6000)` on both probe fetch paths + debug's 6001ms live record |
| Prompt injection | PASS — injection strings via `?q=` handled; agent-field injection covered by parameterized SQL |
| Cost blowup | PASS by construction — prober LIMIT 1 per 20s tick, fastgrader lane zero-network; live blowup test declared skipped |
| Concurrent instances | PASS — worker pidfile lock refuses second instance while holder alive |

### Phase 6: State & Data
WAL mode, `integrity_check` ok (checked 3x including after kill -9), `gradesWithoutAgent=0`, orphan probe_log refs 0, verify-claims exit 0.

### Phase 8: Recovery — worker kill -9 (headline finding)
The worker runs **embedded in next-server via `src/instrumentation.ts` -> `startWorker()`**. Sequence proven: kill -9 the live holder -> app down (000) -> standalone `run-worker.mts` starts, detects stale pid via signal-0, takes over pidfile (48350 -> 51179), resumes grading (2958 -> 2983 grades), no duplicate PKs, integrity ok -> dev server restart -> 200 -> verify-claims exit 0.

### Phase 8.5: Self-Falsification + Ablation
- **Feed-a-lie test committed** (`9168e15`): `tests/falsify/garbage-endpoint.test.ts` — dead agent claiming an endpoint must grade F with 0 liveness; passes (F/17), asserts red on a lying grader.
- **No-op ablation**: `letterFor` no-oped to always-"A" on scratch copy -> `npm run test:unit` **exit 1** (`AssertionError: 'A' == 'B'`) -> reverted -> exit 0. Suite is not hollow.
- Abstention: all unobserved paths recorded OUT_OF_SLICE above; none silently passed.

### Honesty Greps
- `grade all|all 310|fully verified` in src/: 0 hits
- Em-dash: 0 in JSX UI copy (remaining hits are server-side console/error strings + the verify-sanctioned honesty wording in lib/probe.ts — directive: do not touch)
- Accusatory wording (scam/fraud/rugpull/malicious): 0 hits

## Fixes Applied
| # | Test ID | File/Commit | Description | Verified |
|---|---------|-------------|-------------|----------|
| 1 | ST-API-1/4 | c0e1ffd (pre-crash) | 4xx-not-500 on ghost ids, int bounds on POST bodies, gradesWithoutAgent invariant | re-verified this run |
| 2 | ST-API-3 | e03ce37 (pre-crash) | bound `page` param — 1e308*30 offset crashed sqlite binding with 500 | re-verified this run |
| 3 | Phase 8.5 | 9168e15 (this run) | falsification test: dead-endpoint agent must grade F | passes (F/17) |

## Unresolved Issues
None.

## Critique Checkpoint
- Elevations checked: 5 — E-1..E-4 verified (E-1 wording grep + live counters; E-2 fastgrader observed grading live during run; E-3 verified by verify_milestone same day; E-4 proven by ghost-session 404 "no stored session handle"); E-5 = sanctioned deferral.
- Implemented this session: 0. Gaps: none.

## Notes for Deploy/Demo
1. **Worker is embedded in the web server** (instrumentation.ts). Deploy must keep the server process alive for grading cadence; kill -9 recovery is proven but stale `data/worker.pid` persists until takeover.
2. Cooldowns for demo pacing: reprobe 20s/agent, overcap 60s/session, activate 60s global.
3. Session 5 live and valid (overcap revert re-proven this run).
