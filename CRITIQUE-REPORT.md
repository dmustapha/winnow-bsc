# Critique Report — Winnow
> Generated by hackathon-critique | 2026-09-09T03:40:00Z
> Hackathon: BNB Chain Build the Era — Smart Money Era | Track: Main + Altana + TermiX + PancakeSwap | Deadline: 2026-09-09 12:00 UTC (~8.5h at critique)
> Mode: autonomous, emergency clock (4–12h adaptation: quick elevations auto-approved, narrative pass compressed). No scope additions recommended — the cut list is law.

## Competitive Positioning
**Assessment:** differentiated

The deliverable is prescribed and the field is saturated (20+ visible repos, 4 submission-grade HIGH threats), so "unique" is off the table by definition. Winnow holds a genuinely differentiated axis: nobody in the roster ships live probes + recomputable evidence + onchain write-back + in-product capped hiring together, and that axis is also a named 1/3 judging criterion (Data Quality). The PRD's competitive table (vs 8004scan/RNWY) is the right instrument; it should be extended one row mentally against the hackathon field, not just incumbents.

### Overlap Zones
1. **Spend-capped revocable hiring** — Marque's "charters" are conceptually the same pitch as Altana sessions. Our differentiator: sessions are Altana-Keystore-enforced onchain (revert on over-cap, shown live), registered in the canonical public Keystore, and qualify for the Altana bounty; Marque's are bespoke. The over-cap-revert demo scene is what separates us — protect it (see E-4).
2. **TermiX report** — AgentEra has one done with 40.6x/163x/604x claims. Our honest-window framing (INVARIANTS: no APY/P&L multiplier without window+risk+receipts) is the counter — but only if the report *explicitly* speaks TermiX's rubric language (see E-3).
3. **Mainnet agents in all 4 categories** — SmartSentinels already has them. Our testnet-first writes are the single biggest competitive weakness (see Drift Scan item 3 / must-fix list).
4. **Probed registry** — KaizenScope probed 66 agents. Our scale answer (full index + thousands graded, growing, recomputable) must be visibly true on the landing page at judging time — which is why the coverage counters and the fast-grade lane (E-2) matter.

### Differentiation Strengths
- Recomputable-evidence structure (grade requires probe_log FK; re-probe button; verify-claims.ts) — no competitor has "re-verify this number in front of me."
- Registry write-back (positive-only attestations any third-party 8004 reader displays) — the adoption argument BNB's own judges respond to (they are acquiring a product).
- Full-index honesty + ungraded≠unlisted — directly answers the "curated 50" attack that raw-count directories invite.
- Bounty stack is coherent, not checkbox: sessions ARE the activate step; PCS strategies ARE two categories; the TermiX report is generated from the marketplace's own agents.

### Differentiation Gaps
- Reference agents' onchain writes are testnet until morning funding (D-1) — vs SmartSentinels' mainnet fleet and Marque's mainnet PCS receipts. Mitigated only by funding + running the DT-2 runbook.
- Category depth for *real* (non-reference) agents rests on a ~30-agent keyword heuristic — honest and disclosed, but thin; the fast-grade lane (E-2) at least gives those pages real grade data.
- ERC-8183 hire + x402 sell (Altana bonus criteria) both unplanned/cut — acceptable under the clock; logged as deferred E-5.

### Multi-Track Eligibility
- Main: **STRONG** (whole product). Altana: **STRONG** (dedicated session layer, Keystore txs, in-product view/revoke; DT-5b branch would downgrade to AT-RISK — honest banner planned). TermiX: **STRONG** (dedicated report script + reference agents; strengthened by E-3). PancakeSwap: **MODERATE** (PCS v3 reads tonight, real PCS actions only after mainnet funding; the "safe automated" framing via session caps is a genuine fit).

### Market Saturation (Copilot/Grid)
Not run — Copilot PAT unavailable and Grid corpus Solana-centric (same degradation intel recorded; roster audit substituted). Saturation evidence comes from the GitHub roster audit: 20+ direct competitors → positioning capped at "differentiated," which survives because the differentiator is concrete and demo-witnessable.

## Sponsor Integration Depth
| Sponsor | Methods Planned | Surface Available | Depth | Gap |
|---------|----------------|-------------------|-------|-----|
| Altana | createClient, createWallet, execute, grantSession, revokeSession, signerFromPrivateKey + Keystore isValidKey reads (7) | +hireErc8183Agent, x402-server, MCP | **deep** (Gold: activate flow architected around sessions) | ERC-8183/x402 bonus paths cut (E-5 deferred) |
| ERC-8004 / AltLayer 8004scan | register, tokenURI, ownerOf, giveFeedback, getSummary + 4 scan API endpoints (9) | verify-endpoint POST, semantic search, leaderboard | **deep** (Platinum-leaning: write-back is novel usage) | none material |
| PancakeSwap | slot0, Quoter, factory getPool (3 reads; NPM ops mainnet-AM) | SmartRouter, MasterChefV3, subgraph | **moderate** | real position ops gated on D-1 funding |
| TermiX | report artifact (no integration required by rules) | — | **compliant** | rubric-language alignment → E-3 |
| Anthropic/AltLLM | haiku reasoning | — | surface (appropriate) | — |

On-chain proof: strong and specific — testnet tx patterns already proven in forge (0x9c1275…, 0xd40ae6…), /proof page + proof.ts + explorer links per chain, wallet addresses in submission. Safety architecture: 4 independent layers (zod / rate limits / circuit breakers / graceful degradation) + onchain layer survives host death. No single-layer designs found.

## Narrative Arc (compressed pass per 4–12h adaptation)
**Score:** compelling

- Problem clarity: strong — 310,215/4%/59.2% is a real, sourced, shocking wedge; the organizer's own words frame it.
- Solution framing: strong — "every number recomputable" is the hardest guarantee, not SDK decoration.
- Demo flow: strong — every scene tagged to a thesis obligation; fallback matrix exists.
- Wow moment: the re-probe live update + over-cap revert. Both depend on code paths fixed/verified by E-4.
- Onboarding friction: 10s PASS (hero + counters), 30s PASS (graded category list), 60s PASS (re-probe works cold, no wallet wall). Judge Experience section is specific, keys-off-host. No WALLs.

## Drift Scan (Phase 3.5)
- **CLAIM↔RUNTIME:** drift found — pitch/UI said "We grade all of them" / "grades the whole registry" while the runtime probes only declared-endpoint agents under a shared 900/day scan budget (worker would never grade the 96% shells; MUST-NOT-CLAIM row 1 violated by our own landing copy). → **E-1 (copy honesty) + E-2 (budget decoupling + shell fast-grade lane) — both applied.** Residual: thesis phrase "grades the whole registry" read as process-in-progress, now backed by the fast-grade lane actually converging on full-corpus grading; compatible, no amendment needed.
- **Numeric drift:** AC-2 "probe ≤15s" vs component worst case ~29s (2×6s checks + paced 15s scan refresh) → resolved by E-2 (scan refresh removed from re-probe path; checks bound = 12s). No other numeric conflicts found (grade thresholds, budgets, caps all consistent across PRD/ARCH/PLAN).
- **CLAIM↔RUNTIME (acknowledged, not elevated):** "Agents surfaced must be LIVE on BSC" vs testnet-first reference agents. *Acknowledged: intentional emergency posture, honestly labeled per chain everywhere (D-6, DT-2, MUST-NOT-CLAIM row 3); the 310K surfaced index IS mainnet; funding runbook D-1 exists with a 10:30 UTC submit-as-is cut.* Flagged below as the top must-do — this is a funding action, not a scope change.
- **Chrome / vanity flags:** chrome scan run. "310,215" hardcoded in layout metadata (would stale + is a claim the UI itself can source live) → folded into E-1, applied. All other headline stats (4%, 59.2%, 77.9%, 175 Keystore keys) carry sources (arXiv 2606.26028, live probes). No unsourced vanity stats remain. Chrome acknowledged.

## Win-Legibility Axes (Phase 4.5)
| Axis | Score | Note |
|------|-------|------|
| Wedge visceralness | strong | Judge presses "Re-probe now" cold and watches the grade recompute in <15s — reproducible in well under 60s |
| Engine-as-truth | strong | Grades are deterministic formula over probe transcripts; LLM writes narration only, skip-and-log on outage, never touches grades or value |
| Advisory / law contract | strong | Value moves only through Keystore-enforced session caps; over-cap revert is a staged demo scene |
| Refuse, don't degrade | strong | Timeout = explicit verdict; LLM down = skipped action; ESCALATE-DON'T-FABRICATE is codified law |
| Hardest guarantee | strong | "Every number recomputable" is structurally enforced (FK + verifier script), not decoration — after E-1 the copy no longer overclaims past it |
| Pitch legibility | strong | PRD "Why this wins" maps 1:1 to the three published criteria; TermiX rubric mapping strengthened by E-3 |

No weak/absent axes → no axis-driven elevations beyond those already raised.

## Elevation Recommendations

### E-1: Honest full-registry copy ("we're grading every one" + live counter)
**Category:** narrative_hook | **Impact:** high | **Effort:** quick | **Risk:** none | **Thesis impact:** compatible
**Status:** approved [AUTO] — applied
Landing h1, layout metadata, demo S1 + VO S1 changed from "We grade all of them" (a MUST-NOT-CLAIM violation our own INVARIANTS forbid and a judge can puncture in one question) to present-progressive "We're grading every one — N so far" backed by the live probed counter. Applied to PRD §6/§6.1, ARCH §14, PLAN T4.2 (grep gate).

### E-2: Probe-budget decoupling + shell fast-grade lane
**Category:** technical_impressiveness (Data Quality criterion) | **Impact:** high | **Effort:** quick | **Risk:** none | **Thesis impact:** compatible
**Status:** approved [AUTO] — applied
Found: every probe burned one 8004scan call (agentDetail refresh), silently coupling probe throughput (4,320/day at 1/20s) to the 900/day scan budget, and the worker never graded the 96% endpoint-less agents (shells would sit "not yet probed" forever — thin category pages, weak coverage counters). Applied: (a) scan refresh only for unindexed rows (also fixes AC-2 ≤15s); (b) new `fastgrader` worker lane batch-grading endpoint-less agents at zero network cost — honest graded coverage grows by thousands/day, the counter story becomes visibly true. ARCH §7/§12, PRD 4.2, PLAN T2.1/T3.2.

### E-3: TermiX rubric-language alignment in the Agent Advantage Report
**Category:** competitive_gap (TermiX 30%+30%+20%) | **Impact:** high | **Effort:** quick | **Risk:** none | **Thesis impact:** none
**Status:** approved [AUTO] — applied
Report template now mandates a per-task "Price + speed vs alternative" line with receipts (Value of services 30%) and the trading-task trio — evaluation window, decision-quality-vs-spot labeled calibration, explicit risk statement (High-stakes criteria 20%) — positioning our honesty directly against AgentEra's 604x-style claims. PRD 7.8, PLAN GATE C5.

### E-4: Persist the Altana session handle inside activateAgent
**Category:** technical_impressiveness (demo obligation c2) | **Impact:** high | **Effort:** quick | **Risk:** none | **Thesis impact:** none
**Status:** approved [AUTO] — applied
ARCH's `activateAgent` never persisted `kv session_handle_{id}` — it lived only in a trailing comment, while `revoke()` and `demonstrateOverCap()` (the revoke + over-cap demo scenes, thesis obligation c2 — the exact surface the first cross-review DISAGREE flagged) hard-require it. A copy-verbatim build ships a broken revoke. Fixed in ARCH §10 (capture lastInsertRowid, atomic kv insert, return sessionId); GATE C3 now checks the kv row exists post-activation.

### E-5: ERC-8183 hire path (Altana bonus) — DEFERRED
**Category:** sponsor_depth | **Impact:** medium | **Effort:** medium (1–2h) | **Risk:** low | **Thesis impact:** none
**Status:** deferred [AUTO] — do NOT build unless C5 finishes early
`hireErc8183Agent` is one atomic SDK call and an Altana bonus criterion (winner-take-all bounty; AgentEra competes). Deferred per emergency clock + Out-of-Scope discipline; if the build runs ahead at C5, this is the single highest-value stretch item — behind the existing early-finish gate only.

## MUST-FIX-BEFORE-SUBMIT (not elevations — existing plan items with raised urgency)
1. **D-1 mainnet funding (~0.01 BNB to 0xc211C942…) + DT-2 runbook** is a de-facto eligibility hedge, not a nice-to-have: "agents surfaced must be LIVE on BSC" + SmartSentinels' mainnet fleet make testnet-only reference agents the most attackable surface in the submission. Honor the 10:30 UTC submit-as-is cut, but treat funding as P0 when Dami wakes.
2. **DT-5 Altana spike stays first in C3 with its 30m box** — the entire Altana bounty and demo scenes c1/c2 sit behind one unverified param name.

## Summary
- Competitive positioning: **differentiated** (saturated field; concrete, demo-witnessable differentiator on a named criterion)
- Sponsor depth: Altana deep, ERC-8004/8004scan deep (write-back = novel usage), PCS moderate (mainnet-gated), TermiX compliant+aligned
- Narrative score: **compelling**
- Elevations proposed: 5 | approved: 4 (all [AUTO], quick/none) | applied to docs: 4 (PRD+ARCHITECTURE+PLAN, tagged) | deferred: 1 (E-5)
- Drift scan: 2 findings elevated (E-1/E-2), 1 acknowledged (testnet-first, honestly labeled), chrome flag fixed (E-1)
- Verdict: **PROCEED** — plan is built to win on its own terms; the clock, not the design, is the main risk. Build C0 now.
